{
  "targets": {
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "parallel": true,
        "commands": [
          { "command": "nx serve webapp" },
          { "command": "nx serve api" }
        ]
      }
    }
  }
}
