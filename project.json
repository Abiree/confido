{
  "targets": {
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "parallel": true,
        "commands": [
          { "command": "nx serve webapp" },
          { "command": "nx serve api" }
        ]
      }
    },
    "prettier": {
      "executor": "nx:run-commands",
      "options": {
        "parallel": true,
        "commands": [
          { "command": "pnpm nx lint webapp --fix" },
          { "command": "nx lint api" }
        ]
        
      }
    }
  }
}
